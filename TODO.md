## v2.5 Improvement Tasks

- [x] Implement resumable stream replay using stored runs and `Last-Event-ID`.
- [x] Emit deterministic SSE event IDs for lifecycle and chunk messages.
- [x] Validate stream `use` plugin identifiers at runtime.
- [x] Correct CORS origin handling when credentials are enabled.
- [x] Apply server-provided `retry` hints on the v2.5 client.
